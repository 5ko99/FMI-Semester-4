#include <iostream>
#include <set>
#include <vector>

using namespace std;
int main() {
    int n,k,a,m;
    scanf("%d %d %d",&n,&k,&a);
    scanf("%d",&m);
    vector<int> shots(m+1);
    for(int i=1;i<=m;++i)
        scanf("%d",&shots[i]);

    return 0;
}
